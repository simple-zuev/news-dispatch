# CI Smoke Validation

This public-safe file exists only to trigger the validation workflow through a pull request.

No private context, personal data, internal data, credentials, infrastructure details, or non-public sources are included.

Expected result:

- required files check passes;
- privacy scanner passes;
- workflow is visible on the pull request.
